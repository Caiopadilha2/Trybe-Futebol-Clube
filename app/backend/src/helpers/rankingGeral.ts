// import leaderBoardService from '../services/leaderBoard';

// const rankingGeral = async (teamName: any) => {
//   const rankingHome = await leaderBoardService.rankingHome();
//   const rankingAway = await leaderBoardService.rankingAway();
//   const findTeamHome = rankingHome.find((team) => team.name === teamName);
//   const findTeamAway = rankingAway.find((team) => team.name === teamName);
// };

// export default { rankingGeral };
